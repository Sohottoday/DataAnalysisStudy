# 얕은 복사와 깊은 복사

# 얕은 복사
# 간접적으로 박스의 주소를 복사하는것. 원본이 바뀌면, 복사한 내용도 바뀜.
#copy()
a = [1,2,3]
b = a
a[0] = 38
print(a)
print(b)
print(id(a), id(b))


# 깊은 복사
# 원본을 복사해서 복사본을 따로 만드는 것. 원본을 변경해도 복사본은 바뀌지 않음.
# deepcopy() , [:]
c = [1,2,3]
d = c[:]
print(id(c), id(d))
c[0] = 38
print(c, d)
